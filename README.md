# PauperDeck

Pauper MTG deck builder built with:

- PHP 8
- Slim Framework
- MariaDB
- SQLite
- W2UI

## Setup

### Install dependencies

composer install

### Import database

mysql -u root -p pauperdeck < database/schema.sql

### Run

Use Herd / Valet or Apache/Nginx.
